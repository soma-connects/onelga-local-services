# Production environment variables for build optimization
GENERATE_SOURCEMAP=false
ESLINT_NO_DEV_ERRORS=true
TSC_COMPILE_ON_ERROR=true
FAST_REFRESH=false

# Disable unnecessary features during build
DISABLE_NEW_JSX_TRANSFORM=false

# Bundle analyzer (set to true only when needed)
ANALYZE_BUNDLE=false
